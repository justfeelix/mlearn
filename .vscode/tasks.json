{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run tests",
            "type": "shell",
            "command": "./venv/bin/python -m pytest -q",
            "group": "test",
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}
